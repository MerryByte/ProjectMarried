{
  "name": "our-wedding-memories",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/server && cp index.html styles.css script.js dist/ && cp -r public dist/ && cp server/index.js dist/server/ && if [ -f config.js ]; then cp config.js dist/; else cp config.example.js dist/config.js; fi && if [ -f .openai/hosting.json ]; then mkdir -p dist/.openai && cp .openai/hosting.json dist/.openai/; fi"
  }
}
